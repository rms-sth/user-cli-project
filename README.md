# How to use git

```shell
git config --global user.name "Ramesh Pradhan"
git config --global user.email "pyrameshpradhan@gmail.com"
git init
git add .
git commit -m "Add python project"

git remote add origin git@github.com:rms-sth/user-cli-project.git
git branch -M main
git push -u origin main
```
